# Managed by HAO
# Service: nginx
# 目标路径: /etc/systemd/system/nginx.service.d/limits.conf
# 写入后执行: systemctl daemon-reload
# limits.d 对 systemd 启动的服务不生效，必须另外给 nginx.service 加 override。
[Service]
LimitNOFILE=65535
