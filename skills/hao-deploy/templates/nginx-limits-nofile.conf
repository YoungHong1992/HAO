# Managed by HAO
# Service: nginx
# 目标路径: /etc/security/limits.d/90-hao-nofile.conf
# 用独立 drop-in 提升 nofile，不要去改 /etc/security/limits.conf 主文件。
* soft nofile 65535
* hard nofile 65535
root soft nofile 65535
root hard nofile 65535
