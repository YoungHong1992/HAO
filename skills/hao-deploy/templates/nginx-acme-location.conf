# Managed by HAO
# Service: nginx
# 目标路径: /etc/nginx/hao-acme-location.conf
# 被各站点的 80 server 块 include。启用 80->443 跳转时，这个 location
# 必须仍然留在 80 上，否则证书续期会失败（acme.sh webroot 校验走 HTTP）。
location /.well-known/acme-challenge/ {
    root /var/www/acme;
}
