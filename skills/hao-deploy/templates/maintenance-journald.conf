# Managed by HAO
# Service: maintenance
# 目标路径: /etc/systemd/journald.conf.d/hao.conf
# 写入后: systemctl restart systemd-journald
# 用 drop-in 而不是改 journald.conf 主文件。
[Journal]
SystemMaxUse=500M
RuntimeMaxUse=100M
MaxRetentionSec=14day
